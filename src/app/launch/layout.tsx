import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Launch Pool | Staqe.App",
  description: "Launch Pool | Staqe.App",
  applicationName: "Launch Pool | Staqe.App",
};

export default Page;
