import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Pool | Staqe.App",
  description: "Pool | Staqe.App",
  applicationName: "Pool | Staqe.App",
};

export default Page;
